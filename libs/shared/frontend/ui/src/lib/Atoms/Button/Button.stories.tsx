import React from 'react';
import { Button } from 'libs/ui/src/lib/Atoms/Button/Button';

export default {
  component: Button,
  title: 'Button',
};

export const primary = () => {
  return <Button />;
};
